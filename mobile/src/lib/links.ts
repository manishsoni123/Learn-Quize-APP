/**
 * Public web pages (docs/site/, served by GitHub Pages).
 *
 * Fill these in once Pages is enabled for the repo — the profile rows that
 * link to them stay hidden while a URL is empty, so the app never ships a
 * dead link.
 */
export const links = {
  /** e.g. https://<user>.github.io/Learn-Quize/privacy-policy.html */
  privacyPolicy: '',
  /** e.g. https://<user>.github.io/Learn-Quize/support.html */
  support: '',
} as const;
